{
  "name": "lucaslei-app",
  "version": "1.0.0",
  "description": "Lucaslei 统一应用",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}
